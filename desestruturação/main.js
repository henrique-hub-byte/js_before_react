let canditado = {
    nome : 'Luis',
    idade : 16,
    cargo : 'Junior',
}

/* const n = canditado.nome
const i = canditado.idade
const c = canditado.cargo
 */

const {nome, idade, cargo } = canditado
console.log(`${nome}, tem ${idade} anos, e é dev ${cargo} ainda`)